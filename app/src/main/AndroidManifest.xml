<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.immortal.multiscreen">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- <uses-permission android:name="android.permission.INTERNET"/> -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- <meta-data android:name="com.facebook.sdk.ApplicationId" -->
        <!-- android:value="@string/facebook_app_id"/> -->

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GuessNumber"
            android:label="Вгадай число" />
        <activity
            android:name=".PickBackground"
            android:label="Вибір фону" />
        <activity android:name=".FourLab" />
        <activity android:name=".ProgressBarExamples" />
        <activity
            android:name=".GoogleReSearch"
            android:label="GoogleReSearch" />
        <activity
            android:name=".PhotoSlider"
            android:label="Галерея" />
        <activity android:name=".Fullscreen_View" />
        <activity
            android:name=".GuessFlag"
            android:label="Тест" />
        <!-- <activity android:name=".FacebookJSON"></activity> -->
        <activity android:name=".AdressBook"
            android:label="Adress Book"/>
        <activity android:name=".ViewContact"
            android:label="Перегляд контакту"/>
        <activity android:name=".AddEditContact"></activity>
    </application>

</manifest>